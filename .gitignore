# dependencies
/node_modules

# next.js
/.next/
/out/

# production build
/build

# the SQLite database holds REAL employees + leads + bcrypt hashes — never commit
woodchuckers.db
woodchuckers.db-wal
woodchuckers.db-shm
woodchuckers.dev.db
woodchuckers.dev.db-wal
woodchuckers.dev.db-shm
*.db
*.db-wal
*.db-shm

# secrets — never commit (everything except the checked-in template)
.env*
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# impeccable design-skill scratch — shelved (keep out of git)
.impeccable/

# generated email preview (regenerate with: npm run preview:email)
lead-email-preview.html
